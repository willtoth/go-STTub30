# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.lib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependancies which are not pushed
Sources
minwindef.h
